#lang typed/racket

;; ADVENT OF CODE 2021
;; qxu21

(: list-from-file : Input-Port -> (Listof String))
(define (list-from-file in)
  (for/list ([l (in-lines in)]) l))

(: string->integer : String -> Integer)
(define (string->integer s)
  (let
        ([n (string->number s)])
      (if
       (and n (exact-integer? n))
       n
       (raise "Non-integer in input!"))))

(: integer-list : Input-Port -> (Listof Integer))
(define (integer-list in)
  (map string-integer (list-from-file in)))